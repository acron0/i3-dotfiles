#!/bin/bash
status=$(pomodoro status 2>/dev/null)
if [ -z "$status" ]; then
    echo "n/a"
    exit 0
fi
timer=$(echo "$status" | head -n1 | grep -oE '^[0-9]+:[0-9]+')
desc=$(echo "$status" | sed -n '2p')
echo "$desc | $timer"
